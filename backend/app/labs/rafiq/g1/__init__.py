"""G1 — MOONSHOT. Rafiq's sixth book, replacing F2.

`strategy_G1.py` is the pure rule set and `learning.py` the adaptive layer,
both as delivered; `strategy_G1.json` is the canonical config. The runner in
`app.labs.rafiq.service` drives them — nothing in this package touches a
database.
"""
