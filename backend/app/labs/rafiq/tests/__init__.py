"""The lab's own tests, run with `pytest app/labs/rafiq/tests`.

They live inside the package rather than under `backend/tests/` so the lab
stays one directory, and so `pytest` with no arguments — which the repo scopes
to `testpaths = ["tests"]` — is unchanged by this module's existence.
"""
