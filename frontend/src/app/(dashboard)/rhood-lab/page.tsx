/**
 * The Robinhood Chain recorder's route. One re-export, the way the Forex and
 * Graduation labs' are — the page itself lives in `src/labs/rhood/`.
 */
export { RhoodLabPage as default } from "@/labs/rhood/page";
