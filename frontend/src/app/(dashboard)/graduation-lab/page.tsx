/**
 * The Graduation Lab's route. One re-export, the way the Breakout lab's is —
 * the page itself lives in `src/labs/graduation/` so the whole lab is one
 * folder.
 */
export { GraduationLabPage as default } from "@/labs/graduation/page";
