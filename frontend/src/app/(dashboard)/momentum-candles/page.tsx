/**
 * The Momentum Lab's route. A re-export: the lab lives in `src/labs/momentum/`.
 *
 * Not `/momentum-lab`: that route belonged to a different, deleted lab
 * (Momentum V2) and `hq.test.tsx` keeps it retired.
 */
export { MomentumLabPage as default } from "@/labs/momentum/page";
