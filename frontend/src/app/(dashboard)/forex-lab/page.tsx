/**
 * The Forex Lab's route. One re-export, the way the V6 and Graduation
 * labs' are — the page itself lives in `src/labs/forex-lab/` so the whole lab
 * is one folder.
 */
export { ForexLabPage as default } from "@/labs/forex-lab/page";
