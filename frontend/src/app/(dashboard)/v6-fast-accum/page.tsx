/**
 * The V6 Fast-Accumulation Lab's route. One re-export, the way the Graduation
 * lab's is — the page itself lives in `src/labs/v6-fast-accum/` so the whole
 * lab is one folder.
 */
export { V6FastAccumLabPage as default } from "@/labs/v6-fast-accum/page";
