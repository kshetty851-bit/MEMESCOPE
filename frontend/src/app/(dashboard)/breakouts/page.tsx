/**
 * The NSE Breakout Tracker's route. One re-export, the way the Breakout Lab's
 * is — the page itself lives in `src/labs/nse-tracker/` so the whole lab is
 * one folder.
 */
export { NseTrackerPage as default } from "@/labs/nse-tracker/page";
