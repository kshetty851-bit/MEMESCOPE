/**
 * The Breakout Lab's route. One re-export, the way the Rafiq lab's is — the
 * page itself lives in `src/labs/breakout/` so the whole lab is one folder.
 */
export { BreakoutLabPage as default } from "@/labs/breakout/page";
